# Override for the chat endpoint URL. Defaults to same-origin /api/chat,
# which the Vite dev proxy routes to the Functions emulator and Firebase
# Hosting rewrites to the deployed Cloud Function. Only set this for
# non-Firebase deploys or to point at a remote Functions emulator.
# VITE_API_URL=http://localhost:3000/
