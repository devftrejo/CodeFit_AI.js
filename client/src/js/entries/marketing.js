// Entry for marketing pages (index/landing, about, contact).
// Deliberately minimal — no CodeMirror, no chat, no off-canvas navbar.
import "@fortawesome/fontawesome-free/css/all.min.css";

import "../theme.js";
import "../top-bar.js";
