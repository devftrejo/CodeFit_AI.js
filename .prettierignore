# Installed npm dependencies
node_modules

# Build output
dist

# Lockfiles — managed by npm
package-lock.json

# Git internals
.git

# Local secrets
.env

# Firebase Agent Skills — vendored content, not ours to reformat
.agents

# Firebase-generated config (JSONC with comments, not strict JSON)
firestore.indexes.json

# Firebase CLI deploy cache
.firebase
